"""
上下文压缩。

两层，按"能不能单独测"切开：

- `policy`：纯逻辑（估算 / 切点 / 序列化 / 摘要提示词 / 失败冷却），只依赖消息与标准库
- `runtime`：接进图的那部分（超没超线、调摘要模型、把结果写进状态）

`__init__` 刻意不做 re-export：调用方显式写 `from app.agent.compaction.policy import ...`，
一眼看得出用的是哪一层，也不会长出一个"什么都能从包名拿"的入口。
"""
